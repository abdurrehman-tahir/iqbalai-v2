"""User endpoints — stub for T-007. Full CRUD in M-01."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/users")

# Endpoints added in M-01 per WORKFLOW.md
