/**
 * T-092 — Exam Frameworks admin page (Platform Admin).
 * List + create + edit + delete. Edit/delete gated to DRAFT status server-side.
 */
import { FrameworksClient } from "./FrameworksClient";

export default function ExamFrameworksPage() {
  return <FrameworksClient />;
}
