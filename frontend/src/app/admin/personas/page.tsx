/**
 * T-021 — Teaching Personas admin page.
 * 4 named personas + 1 Custom slot. Admin can edit system prompt (8000 chars).
 */
import { PersonasClient } from "./PersonasClient";

export default function PersonasPage() {
  return <PersonasClient />;
}
