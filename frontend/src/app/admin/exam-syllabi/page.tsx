/**
 * T-020 — Exam Syllabi admin page.
 * List + create + edit + delete. Versioned — editing creates v(n+1).
 */
import { SyllabiClient } from "./SyllabiClient";

export default function ExamSyllabiPage() {
  return <SyllabiClient />;
}
