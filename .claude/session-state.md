# Session state (live)

**Fixed:** Grades 422 — no active academic session on Sample School.
**Live:** inserted session `2025-2026` for Sample School + set `active_academic_session`.
**Durable:** `scripts/seed_dev.py` now also seeds that sample-school session.
**Next for Hamza:** refresh Grades (or Retry) as school.admin / coordinator.
