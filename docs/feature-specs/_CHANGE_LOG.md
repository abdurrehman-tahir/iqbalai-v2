




# Change Log — IqbalAI v2 drafting + bootstrap maintenance

**Purpose:** Running chronological record of every change made during the v2 drafting effort — flow specs, milestone backlogs, and bootstrap/process docs (CLAUDE.md, WORKFLOW.md, STACK_LOCK.md, BRANCHING.md, skills, ARCHITECTURE.md, TODO.md, ENV_VARS.md, AMENDMENTS.md). It answers "what changed, when, and why."

**Status & lifecycle:** Kept in the bootstrap as a permanent audit trail. This is a *conscious override* of this file's original design — it began life as a temporary "Non-Spec Doc Updates **Pending**" queue that captured flow-review decisions to drive a one-time batch update of the non-spec docs, then was to be deleted. That batch happened (the T0 batch); every queued edit landed (see the historical note below). Rather than delete the file, we repurposed it as the running change log, because the chronological record proved valuable for cross-session continuity. **`AMENDMENTS.md` remains the canonical record of architectural amendments;** this log is the "what/when/why" trail beside it.

**Pre-draft-audit dependency (do not break):** the milestone pre-draft audit greps this file for `BLOCKED-HOOK` (see `docs/backlog/README.md`). Keep deferral mentions greppable here.

---

## Historical note — Flow 1-4 review batch (the original "pending" queue, now applied)

All non-spec-doc edits captured during the Flow 1-4 review were **applied during the T0 batch** and verified present (2026-05-29 reconcile). They were renumbered in that batch; old→new section map for anyone following an old reference:

- §6.16 → **§6.19** Permission inheritance
- §8.16 → **§8.20** Custom Persona learning batch
- §8.18 → **§8.22** Vision-LLM routing
- §9.14 → **§9.21** Notification namespaces
- §11.17 / §11.18 → **§11.19** Upload profiles (consolidated)
- §3.16 (independent dual-schema), §3.17 (subscription), §3.18 (Grade/Section/Subject), §3.19 (Exam Framework), §4.21 (dual Alembic heads), cross-grade unidirectional retrieval, independent signup path, promotion approval workflow, exam-framework refresh agent — all present.

Permanent amendment record for this batch: **`AMENDMENTS.md` Entry 1**. The ~55 deferrals went to `TODO.md`; `PERSONA_LEARNING_BATCH_DAYS` to `ENV_VARS.md`; STACK_LOCK confirmations applied.

## Open stack items for not-yet-drafted flows (resolve at that flow's pre-draft)

- **Flow 5 / TipTap — RESOLVED (2026-05-29):** TipTap added to `STACK_LOCK.md` §2 (Frontend) with JSON-versioning rationale + a no-Pro/CRDT guard. (Note: it belongs in §2 Frontend — earlier notes mis-said §12, which is the Change log, and flow-5 §10 lumps it under §4; harmless cross-ref drift.)
- **Flow 11 #103 group shared notes → Yjs CRDT** is NOT in STACK_LOCK; needs a deviation decision when Flow 11 / M-20 is drafted.
- **Flow 8 Google Calendar** integration may conflict with the no-GCP stack stance; reconcile at M-17 pre-draft.

## Known doc bug — RESOLVED (2026-05-29)

- **Duplicate `### 3.16` header in ARCHITECTURE.md** — the stray "Deferred to TODO.md" block was demoted from a numbered `### 3.16` header to a bold note under §3.15 (it was an orphan — nothing referenced it by number). §3.16 now unambiguously means "Independent users — separate-schema tenant model" (the load-bearing, widely-referenced section).

---

## Change log

| Date | Change | Author |
|---|---|---|
| 2026-05-14 | Ledger created and backfilled with decisions from Flows 1-4 review rounds | Backfill complete; covers ~28K tokens of conversation |
| 2026-05-14 | Flow 3 v3 + Flow 4 v2 drafted and synced to outputs | Spec files updated; ledger covers all non-spec changes pending |
| 2026-05-14 | Flow 4 v3 drafted (incorporates v3 feedback: profile mandatory trim, independent diagnostic, Exam Framework AI engine, 6-month graduation+migration) | Major additions to pending ledger: §3.19 Exam Framework engine, §8.17 quarterly refresh agent, new Celery beat tasks, new env var GRADUATION_GRACE_DAYS, new NATS events, new alumni-status concept in school dashboards |
| 2026-05-14 | Flow 5 v1 drafted (Teacher Creates a Lecture — 19 features) | Pending ledger additions: §7.13 cross-grade unidirectional retrieval weighting, env var AI_ADAPT_QUESTION_THRESHOLD (default 2 — actually Flow 6's), upload profile lecture_image (5MB JPEG/PNG/GIF), NATS events for lecture generation/versioning/scoring/originality, Celery tasks for scoring/originality/topic_relevance/tips/benchmarks, beat schedule benchmark.update_weekly + quiz.generate_for_late_enrollment, independent teacher stripped variant in §3.16 already-locked, plagiarism flag system notification |
| 2026-05-14 | Flow 6 v1 drafted (Student Studies a Lecture — 9 features including #60 NATS pipeline) | Pending ledger additions: §9.x NATS JetStream event pipeline finalization (subject pattern student.lecture.*, three-consumer fan-out), env var AI_ADAPT_QUESTION_THRESHOLD (default 2), Custom Persona + Adaptation interaction rule (persona = style, adaptation = strategy), concept_applications shared cache (school-tenant), student_events table partitioned monthly 7-year retention, hybrid voice widget #57 as canonical reusable component (frontend-master skill reference), privacy #72 inviolate (no role can override), TTS audio cache lifecycle (invalidate on lecture re-edit) |
| 2026-05-14 | Flow 1 v2 + Flow 2 v3 + Flow 13 NEW drafted | All decisions from review rounds now in permanent spec form. Flow 1 v2: Custom Persona (4 named + 1 Custom), multi-tier content library, English-only disclaimer, 7-namespace notifications, all 6 roles created from start. Flow 2 v3: Grade/Section/Subject restructure, promotion workflow with exclusion + approval, manual signup for independents only (Path B), permission inheritance matrix. Flow 13: thin subscription spec, schema-only at launch, placeholder UI, Stripe deferred. |
| 2026-05-14 | Flow 6 v2 (Awais feedback: multimodal image attachments) | New upload profile `student_question_image` in §11.17 (5MB JPEG/PNG/WEBP, EXIF stripped, MinIO scoped per-tenant, 1-year retention); vision-LLM routing in §8 (Groq Llama-3.2-Vision when `attached_images[]` present); NATS event envelope #60 includes optional `attached_images[]` field; cross-flow propagation to Flow 5 wizard chat sidebar + Flow 7 review + Flow 8 self-study widget |
| 2026-05-14 | Pre-T0 consolidation pass | Added AI_ADAPT_QUESTION_THRESHOLD env var (Gap 1); added §7.13 cross-grade RAG retrieval weighting architecture entry (Gap 5); added consolidated Celery beat schedule section with 10 tasks (Gap 2); added consolidated NATS event subjects section with ~50+ events grouped by family (Gap 3); added consolidated DB tables section with ~50+ tables grouped by owning flow + tenant (Gap 4). Ledger is now T0-ready. |
| 2026-05-14 | **=== BATCH POINT T0 — APPLIED ===** All ledger contents propagated to bootstrap files: ARCHITECTURE.md (§0.1, §3.16-3.19, §4.21-4.23, §6.19-6.23, §7.21-7.23, §8.20-8.24, §9.3 event taxonomy, §9.21-9.23, §10.6 beat schedule, §11.19-11.22 — actual numbering required renumbering existing tail subsections to accommodate aspirational ledger numbers); AMENDMENTS.md (A-001 entry); TODO.md (55+ Phase 2/3 deferrals); ENV_VARS.md (NEW file, 9 env vars); STACK_LOCK.md (4 confirmations); CLAUDE.md (tenant types paragraph + ENV_VARS.md required reads); HAMZA_START_HERE.md (§3.1 tenant types section); feature-specs/README.md (flow-based organization note); _TEMPLATE.md (tenant column in personas, inheritance header in permissions, namespace guidance in notifications); skills (frontend-master/references/hybrid_input_widget.md NEW, phase-complete-review Checklist N, stack-enforcer canonical examples extended). Spec files updated via bulk sed to match real section numbers (§6.16→§6.19, §8.16-18→§8.20-22, §9.14→§9.21, §11.17-18→§11.19-20, §7.13→§7.21). | @abdurrehman (with Claude) |
| 2026-05-20 | **Bootstrap cleanup pass (pre-M-03).** Collapsed three docker-compose files (`docker-compose.{infra,app,override}.yml`) into ONE `docker-compose.yml` at the repo root. Removed `Makefile` lock — raw `docker compose` commands are the operator interface. Collapsed split env files (`api/.env.example` + `frontend/.env.example`) into ONE `.env.example` at the repo root. Removed `docker/` subdirectory references — Dockerfiles live under `api/` and `frontend/`, observability config at top-level (`grafana/`, `prometheus/`, etc.). Updated: ARCHITECTURE.md §15.1, §15.2, §15.4, §15.16 + file tree at line 406; STACK_LOCK.md §1.4 compose layout. Rewrote `local-dev-setup.md` from scratch (390→253 lines) — now starts with "if M-00 hasn't shipped yet, there's nothing to set up; come back when `docker-compose.yml` exists at repo root." Converted `first-time-setup.md` to a deferred stub (343→53 lines) — fills in on first real VM provision post-M-23. Rewrote root `README.md` for milestone workflow + simplified Docker model. Updated `phase-complete-review/references/file_section_map.md` to drop split-file mappings. Reason: original scaffolding referenced files that didn't exist — Hamza would have hit walls on Day 1. Single-compose + no-Makefile keeps the surface area small and the failure modes obvious. | @abdurrehman (with Claude) |
| 2026-05-20 | Flow 7 v1 drafted (Next-Day Review Loop — 8 features #39,#40,#44,#45,#46,#47,#48,#49). School-tenant only. Mini-lecture = lecture_type=mini variant reusing Flow 5 pipeline; overnight aggregation 23:00 Karachi → 6am SLA; per-Grade-Subject reviews; targeted distribution; reflective prompt coaching-only (CXO lock); privacy #72 enforced. New tables lecture_reviews, review_replies, review_classification_failures; lectures gains lecture_type + parent_lecture_id. | @abdurrehman (with Claude) |
| 2026-05-20 | Flow 8 v1 drafted (Student Self-Study Mode — 10 features #62,#63,#64,#65,#66,#67,#68,#69,#70,#72). FIRST genuinely dual-tenant core flow (school toggle-in + independent locked-in). Owns canonical privacy #72. Corrects v2 doc external-service errors against STACK_LOCK: PaddleOCR not Google Vision, Jazz/Telenor not Twilio, Brevo not SendGrid, FullCalendar built-in, py-fsrs deck (scheduling deferred to Flow 9). New tables student_materials, student_goals, prep_book_topics, study_plans, study_plan_items, study_plan_days, reminder_preferences. New beat tasks check_adherence/refine_granularity/send_session_reminders. Flagged open Q: Google Calendar API vs STACK_LOCK no-GCP line (needs Abd. ruling). | @abdurrehman (with Claude) |
| 2026-05-20 | ARCH §3.18 stale-ref fix (§6.16→§6.19, §7.13→§7.21 — leftovers from T0 renumbering). M-03 backlog drafted (Subjects + Grade/Section/Subject offerings — 12 tickets T-041..T-052, source Flow 2 v3 §3.2-§3.3). Covers Subject catalog, Academic Session, Grade, Section (optional + default-internal), GradeSubjectOffering, Teacher assignment with capacity (cap 5, override audit), cross-grade unidirectional guard, scope/inheritance enforcement, notifications, audit, E2E, milestone PR. Student enrollment deferred to M-06; promotion to M-22. ROADMAP M-03 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-20 | M-04 backlog drafted (Teacher Onboarding + school-tier Content Library — 16 tickets T-053..T-068, source Flow 3 v3 #11/#16/#17/#18, school tier only). Covers school teacher onboarding + READY_TO_TEACH derivation, content library data model, school_library_content upload + per-school dedup, RAG ingestion Pattern S (parse→chunk→embed→Qdrant), curriculum structured topic_tree parsing, curriculum upload (always public) + reference book upload (privacy toggle default private, private→public one-way), library browse/search/tag-filter, ingestion status/retry, teacher self-edit capacity, cross-grade visibility (reuses T-047 guard), soft-delete with citation retention, content_library notifications, audit, E2E, milestone PR. Independent teacher onboarding + Platform Library deferred to M-05; lecture creation to M-09+. ROADMAP M-04 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-20 | M-05 backlog drafted (Independent Users signup + Platform Library — 8 tickets T-069..T-076, source Flow 3 v3 §3.2 + Flow 4 v3 §3.2 + Flow 1 Platform Library). Covers independent signup path (Authentik group + tenant_type JWT + schema routing), independent teacher onboarding (READY_TO_USE), independent student onboarding (exam framework REQUIRED + exam date, diagnostic deferred to M-08), Platform Library upload (platform_reference_book global dedup) reusing M-04 ingestion, Platform Library read-only access for all tenants + cross-schema views, independent private pool (always private), E2E (schema isolation asserted), milestone PR. Diagnostic execution → M-08; self-study/study-plans → M-08+; lecture creation → M-09+. ROADMAP M-05 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-20 | M-06 backlog drafted (School Student Onboarding + Parent Linking — 14 tickets T-077..T-090, source Flow 4 v3 §3.1/§3.3/§3.7/§3.8/§3.9). Covers school student data model + Coordinator enrollment into Grade-Section, student onboarding (trimmed mandatory fields, mode pick, READY_TO_STUDY), bulk CSV enrollment (implements M-02 T-039 skeleton), parent registration, parent→student link request + student approval (§6.13 consent), revocation + multi-parent/child, exam date capture (#52), data rights (#15 export/delete), graduation 6-month school-read-only window, atomic auto-migration to independent tenant (Celery beat + 5x retry + parent sever + alumni retention), notifications, audit, E2E, PR. Exam Framework engine → M-07; diagnostic + full Mode Switcher → M-08; lecture creation → M-09+; parent monitoring → Flow 10. Highest-risk ticket T-086 (atomic migration). ROADMAP M-06 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-20 | ARCH §3.19 stale-ref fix (§8.17→§8.21, T0 renumber leftover). M-07 backlog drafted (Exam Framework Engine, platform tier — 10 tickets T-091..T-100, source Flow 4 v3 §3.5 + ARCH §3.19/§8.21). Covers framework data model (platform-shared, cross-schema views), Platform Admin DRAFT CRUD, Pattern-A AI research agent (SearXNG→web_fetch→LLM synthesis→JSONB study plan, cost ceiling), manual approval workflow (72hr SLA + escalation), versioning + quarterly refresh (Celery beat framework.refresh_quarterly) + deprecation, student selection + study-plan rendering + region scoping + self-study hook, notifications, audit, E2E, PR. Lecture Mode overlay UI deferred to M-09+ (hook only); diagnostic → M-08; study-plan execution → Flow 8/M-08+. Heaviest ticket T-093 (first real Pattern-A agentic run). ROADMAP M-07 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-20 | M-08 backlog drafted (Student Mode + Diagnostic + Cognitive DNA SEED — 12 tickets T-101..T-112, source Flow 4 v3 §3.4/§3.6/§3.7). Covers full Mode Switcher (school toggle, independent locked 404), provisional cognitive_dna seed store (per-tenant), diagnostic data model + lifecycle (save/resume 7d, 30d cooldown), LLM diagnostic question generation (per-subject school / per-framework independent; Question Bank #74 deferred hook), diagnostic UI with coaching-only results (never grades), diagnostic→DNA seeding, exam-date countdown (#52), mode-conditional dashboard + adaptation read-hook, notifications, audit, E2E, PR. CAUTION flagged: full Cognitive DNA engine (mistake tracking, pass prob, spaced rep, forgetting curve, Question Bank) is Flow 9/M-18 (BLOCKED, not drafted); M-08 builds seed only; soft dependency noted — DNA schema provisional, may need follow-up migration when Flow 9 drafted. ROADMAP M-08 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-20 | M-09 backlog drafted (Lecture Wizard + AI Generation, Pattern S RAG — 16 tickets T-113..T-128, source Flow 5 v1 §3.1/§3.2/§3.4/§3.13/§3.14/§3.15/§3.16). Covers lecture data model (lectures/versions/drafts/paragraphs; lecture_type+parent_lecture_id Flow7-ready), 5-step wizard (topic from curriculum tree, curriculum confirm, reference selection w/ cross-grade toggle, teaching mode, confirm+estimate, draft auto-save), Pattern S dual-RAG generation (curriculum 1.5x weight, structure vs depth), WebSocket streaming + reconnect, per-paragraph source badges (#26), out-of-curriculum fallback tiers (#27 ref→SearXNG→no-info), exam framework overlay context, voice creation STS loop (#25 faster-whisper/Piper/Edge/AI4Bharat + OT draft edits), cross-grade lecture linking (#21 reuses T-047), per-lecture access (#21), delivery tips/technique/real-world (#28/#41), independent stripped variant (own refs, no auto-quiz), notifications+audit, E2E, PR. Edit+versions+scoring → M-10; auto-quiz+publish → M-11. Also: unblocked M-16 (Flow 7 drafted) + M-17 (Flow 8 drafted) in ROADMAP. ROADMAP M-09 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-20 | CI invariants bound into the bootstrap (triggered by Hamza's M-00 PR CI failures: missing dev-dep install before ruff/pytest, empty frontend package.json, silently-regenerated ci.yml). Added "CI invariants (non-negotiable)" section to .claude/CLAUDE.md (5 rules: dev-deps-before-tools, commands-match-manifest, edit-don't-replace, frontend post-checkout detect not job-level hashFiles, CI changes called out in PR). Added pointer + acceptance check #6 to M-00 T-014 (CI/CD ticket) and fixed its stale WORKFLOW §3.0+ ref → §0+§2.x. Added "Checklist CI" to phase-complete-review SKILL.md Pass 2 (fires when PR touches .github/workflows/). Note: the centrally-shipped ci.yml stays a soft-fail bootstrap placeholder; Claude Code owns the real ci.yml but is now bound by these invariants. | @abdurrehman (with Claude) |
| 2026-05-20 | Stale-ref audit fix: M-05 T-069 cited §6.17 as "Authentik IDP issues both JWT kinds" but §6.17 is actually "Threat model (locked)". Corrected M-05 citation → §6.20 (independent signup path) + §3.16 (separate-schema tenant model, where dual-JWT issuance is actually specced). Also fixed the root-cause cross-ref in ARCHITECTURE.md §3.16 (line ~1714) which itself mis-pointed "issues both kinds of JWTs (per §6.17)" → now (per §6.20). Audit of all 10 drafted milestones (M-00..M-09) otherwise clean: §11.18/§15.4 verified correct, no forward ticket deps, no undrafted-flow citations, ROADMAP statuses correct, no leftover phase/Docker-split/plans language. | @abdurrehman (with Claude) || 2026-05-20 | Context-bloat fix bundle (S1–S5 surgical + A1 sub-agent + M2 session-state). Triggered by Hamza's Claude Code sessions compacting after 2-3 tickets due to per-ticket cumulative reads of milestone + flow spec + ARCH + skills. Changes: (S1) CLAUDE.md "Critical reads" list replaced with conditional-trigger table — STACK_LOCK/WORKFLOW/DEVIATIONS/AMENDMENTS/BRANCHING/ENV_VARS load only when their concern fires, not pre-emptively. (S2) WORKFLOW.md redundant 9-file reading list removed, points to CLAUDE.md as single source. (S3) Skill triggers narrowed to conjunctive conditions: stack-enforcer fires only on (a) non-stdlib import / (b) new dep / (c) app/infrastructure/{rag,ml,llm,voice,storage,events,cache}/ touch; frontend-master fires only on (form / data-fetching / a11y-sensitive / new page / new frontend dep) in .tsx — both SKILL.md frontmatter descriptions updated to match; gates remain enforced at pre-commit + CI regardless. (S4 folded into A1) Ticket discovery moves into the ticket-loader sub-agent; Hamza's "Implement T-XXX" prompt unchanged. (S5) Sections-read tracking moved into the dossier output + session-state file. (A1) New sub-agent .claude/agents/ticket-loader.md — invoked as the literal MANDATORY first action when implementing any ticket; reads ticket + cited spec § + cited ARCH §§ in its own context with hard line-range limits (150/section spec, 80/subsection ARCH), grep-checks AMENDMENTS, verifies Depends-on, returns a compact ~6k-token Ticket Dossier; main session is forbidden from reading milestone/spec/ARCH directly. (M2) New .claude/session-state.md ≤30-line live file Claude Code updates per step (dossier loaded, file edited, commit) for compaction recovery — replaces the need to re-fetch everything post-compaction. CLAUDE.md verbose "Acknowledgment" first-message replaced with silent start. Expected impact: per-ticket main-context footprint drops from ~50-70k to ~15-25k tokens, ~3× improvement, sessions go from 2-3 tickets to ~8-10 tickets before compaction. All gates (pre-commit, CI, PR review) unchanged — correctness is preserved; only unconditional pre-loading of reference material was removed. | @abdurrehman (with Claude) |
| 2026-05-29 | Opus 4.8 + Claude Code May-2026 alignment (triggered by Anthropic's 2026-05-28 release of Opus 4.8 + Claude Code updates: dynamic workflows, /effort xhigh, lean system prompt default, ~4× honesty improvement, fewer multiple-choice prompts). Three files touched, no application code: (1) `.claude/CLAUDE.md` — added "prefer sub-agents / dynamic workflows for parallel exploration" note to the Sub-agents section (formalises the pattern Opus 4.8's dynamic workflows enable); added "Flag uncertainty rather than guess" as a Behavioral rule (leans into Opus 4.8's honesty improvement — explicitly preferred over silently-buggy code); softened the ">5 files in a single change → ask first" rule to "AND not a coherent vertical slice already authorised by the ticket" (Opus 4.8 self-decides well on routine ticket scope, so it shouldn't ask permission for every vertical slice). (2) `docs/HAMZA_START_HERE.md` — new §1.1 "Claude Code model + effort settings" telling Hamza to set `/model claude-opus-4-8` (drop-in for 4.7, same price, honesty + agentic-coding wins on multi-file vertical slices), use `/effort xhigh` for hard tickets only (RAG / migrations / `app/infrastructure/{rag,ml,llm,voice}/`-touching), skip `/fast` mode for architecture-critical work. No changes to STACK_LOCK, WORKFLOW, skills, or the ticket-loader sub-agent — those remain accurate as-is. | @abdurrehman (with Claude) |
| 2026-05-29 | M-10 backlog drafted (Lecture Edit + Versions + 7-Dim Scoring — 12 tickets T-129..T-140, source Flow 5 v1 §3.5-§3.12, features #29-#38). Covers edit data model (lecture_edit_sessions, scores_json/topic_relevance_pct/originality_score columns, teacher_ai_memory, teacher_benchmarks, lecture_originality_index), TipTap editor + immutable new-version-per-save (#29), voice edits via faster-whisper (#29), image upload + AI diagram suggestions via lecture_image profile §11.19 (#30), effort tracking with Visibility-API pause (#31), 7-dimension async scoring as a separate SCORING_MODEL LLM call per §7.10/STACK_LOCK §4.1 (#32), system-wide originality check via BGE-M3 §7.3 + global index, matched-teacher hidden, >0.85 plagiarism flag, independent isolation §3.16 (#33), topic relevance gauge (#34), version score timeline (#35), Teaching Innovation Record adaptive coaching via teacher_ai_memory (#36), anonymized positive-framed benchmarking via locked §10.6 benchmark.update_weekly beat (#37) + scope-restricted admin comparative metrics table §6.19 (#38), notifications+audit+E2E+PR combined into T-140. Auto-quiz + student publish → M-11; cross-grade linking/access already in M-09. Flagged: TipTap to be confirmed in STACK_LOCK §12; teacher_ai_memory distinct from student Cognitive DNA (Flow 7/M-16 extends it later). ROADMAP M-10 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-29 | M-11 backlog drafted (Auto-quiz Per Student + Publish Lecture — 10 tickets T-141..T-150, source Flow 5 v1 §3.3 #23b + §3.1/§3.5 publish lifecycle). Completes Flow 5. Covers quiz data model (quizzes/quiz_questions/quiz_assignments-per-student/quiz_attempts, per-question source attribution §4106), lecture publish lifecycle (→PUBLISHED, visible to enrolled Grade-Subject students, access-respecting, admin override §6.19), auto-quiz per-student parallel generation (Pattern S, quiz_gen queue, 5-10 Q, 2-min class SLA #23b), per-student calibration off M-08 diagnostic seed with Cognitive-DNA hook, quiz publish bound to lecture publish + student dashboard, attempt+idempotent scoring+immediate results (student.quiz.completed), results visibility/aggregates per §6.19, late-enrollment event-driven generation (quiz.generate_for_late_enrollment on student.enrolled) + independent no-quiz exclusion, notifications+audit (T-149), E2E+PR (T-150). CAUTION flagged: calibration soft-depends on Flow 9/M-18 (BLOCKED) — built against M-08 seed via clean interface, grade-default fallback when no seed; DNA mastery map NOT built here. Quizzes are real assessments (distinct from coaching-not-grading rule). Lecture consumption → Flow 6/M-12. ROADMAP M-11 status → drafted. | @abdurrehman (with Claude) |
| 2026-05-29 | Encoded the milestone-drafting pre-draft audit as a MANDATORY routine (previously only a working convention). docs/backlog/README.md "Drafting cadence" gains: (1) a 5-step "Mandatory pre-draft audit" run before drafting any milestone — verify sources on disk, grep BLOCKED-HOOK + fold inherited hooks into the milestone as real tickets, stale-ref pass, dependency sanity, then draft; (2) a standardized greppable deferral marker `BLOCKED-HOOK: <stub> → <target flow/milestone> (built against <interim> for now)` so a single grep finds the complete set (no dedicated index file needed). Retrofitted the marker onto existing deferrals: M-11 T-144 (Cognitive DNA calibration) + M-08 milestone notes (Cognitive DNA engine/schema). WORKFLOW.md given a pointer to the routine. Decision: keep the lightweight per-file + grep approach rather than a separate BLOCKED-DEPENDENCIES.md index — same loss-prevention with less rigidity/sync-drift; revisit the index only if hooks start slipping. | @abdurrehman (with Claude) |
| 2026-05-29 | Mirrored the pre-draft-audit pointer into .claude/CLAUDE.md (now the workflow source-of-truth; WORKFLOW.md read only when something's missing). Added a short "Drafting vs implementing (scope note)" after the implementer workflow loop: clarifies CLAUDE.md governs implementation, milestones are drafted separately with a MANDATORY pre-draft audit (verify sources → grep BLOCKED-HOOK + fold inherited hooks → stale-ref pass → dependency sanity) documented in docs/backlog/README.md, and that the audit must run first if asked to draft/extend a milestone. Kept it a short pointer (not the full 5-step block) to avoid re-bloating the always-loaded CLAUDE.md per the context-bloat fixes. | @abdurrehman (with Claude) |
| 2026-05-29 | Back-sweep of all milestones (M-00..M-11) for deferrals targeting still-blocked flows, after recognizing earlier milestones were drafted while different flows were blocked. Findings: (Flow 9/M-18) M-08 T-104 Question Bank #74, T-106 student.diagnostic_completed consumer, T-102 provisional cognitive_dna schema were deferred to Flow 9 but unmarked — retrofitted BLOCKED-HOOK markers (ticket-level + milestone notes). (Flow 10/M-19) M-06 parent read-only monitoring surface/dashboards/digests deferred to Flow 10 but unmarked — added BLOCKED-HOOK to M-06 milestone notes. Confirmed M-08 T-108 adaptation hook targets Flow 6/Flow 8 (DRAFTED), so deliberately NOT marked (marking it would wrongly hide it). Also closed an audit gap the sweep exposed: forward hooks to draftable-but-not-yet-drafted flows (Flow 6/7/8 → M-12-17) are invisible to the BLOCKED-HOOK grep, so added a second grep to the pre-draft audit (docs/backlog/README.md step 2): grep "Flow <N>" for the flow being drafted, to catch hooks planted before that flow was drafted. Registry now complete: Flow 9 hooks + Flow 10 hook all tagged. | @abdurrehman (with Claude) |
| 2026-05-29 | Reverted two misplaced drafting-side pointers from the implementer-facing files: removed the "Drafting vs implementing" scope note from .claude/CLAUDE.md and the Step-2 drafting note from docs/WORKFLOW.md. Rationale: the pre-draft audit + BLOCKED-HOOK convention are drafting-side concerns (Abd. + Claude), not Hamza's Claude Code implementation concerns — their home is docs/backlog/README.md, and roadmap/backlog state lives in ROADMAP.md + milestone files. CLAUDE.md and WORKFLOW.md retain only implementation-side changes (context-bloat bundle: conditional-reads table, ticket-loader-first rule, session-state recovery, silent start, narrowed skill triggers, WORKFLOW reading-list dedup; Opus 4.8 alignment: flag-uncertainty, dynamic-workflows preference, softened >5-files rule). Delivered as one clean consolidated version of each file. | @abdurrehman (with Claude) |
| 2026-05-29 | Cross-session resume + per-ticket close-out enforcement + Project-state rewrite (all in CLAUDE.md, the always-loaded file, so it's enforced regardless of conditional WORKFLOW reads). (1) Rewrote the stale "## Project state" block (was "Architecture complete / Phase 1 implementation plan per WORKFLOW Step 1" — dead phase-based language that auto-loaded every session + leaked into PR text) → now points at ROADMAP.md as state-of-truth. (2) Made per-ticket close-out MANDATORY every ticket (not just at milestone close): set ticket Status:done + record commit SHA in the milestone file, then update session-state.md. (3) Added a session-start Resume rule: read ROADMAP → active milestone → first not-done ticket; session-state.md is a fast hint; if they disagree the milestone file's Status fields WIN (durable source of truth). Mirrored the close-out into WORKFLOW.md §1.3 (commit SHA recorded; milestone file named the durable resume ledger). Net: a fresh Claude Code session knows exactly which ticket to start from with zero human pointer, and the enforcement lives where it's always seen. NOTE: stale phase language also reported in the PR template/description text — scrub there too (separate file, not in bootstrap docs). | @abdurrehman (with Claude) |
| 2026-05-29 | Ticket-status enforcement — SCOPED DOWN to CI-verifier-only (the full hook bundle was NOT adopted). Decision: per-ticket close-out enforcement was proposed as a post-commit writer hook (--amend, auto-writes Status) + a CI verifier. Mid-M-01 (M-00 already merged), the post-commit --amend writer was judged too risky/complex for an in-flight workflow (rewrites local commit SHAs, rough edges with rebases/multi-commit tickets/uninstalled hooks) against a mild, self-correcting problem. SHELVED: scripts/update_ticket_status.py + .githooks/post-commit (writer/hook) — parked, not applied. ADOPTED: scripts/check_ticket_status.py as a read-only PR check (new ticket-status-check job in ci.yml) — fails a PR if any ticket referenced by its commits isn't Status:done; docs-only PRs skip; touches nothing in the running workflow, cannot rewrite history. Per-ticket done-marking remains a manual/Claude step (the existing WORKFLOW close-out) for now. Token overhead: none in Claude Code's per-session context (CI runs outside the model). NOTE: the earlier bootstrap-doc edits that referenced the shelved hook (STACK_LOCK git-hooks row mentioning update_ticket_status.py post-commit; WORKFLOW §1.3 "automated by .githooks/post-commit" + trailer convention; M-00 acceptance core.hooksPath + post-commit items) describe machinery that was NOT adopted — those edits should be trimmed to verifier-only or reverted to avoid stale/contradictory docs. Revisit the writer hook only if a real mis-resume is observed. | @abdurrehman (with Claude) |
| 2026-05-29 | Reconciled stale phase-based branch naming in docs/BRANCHING.md to the current milestone model (it conflicted with WORKFLOW.md's milestone/M-NN; was the upstream source of the same staleness fixed in the repo PR template). Changes: section heading "Feature, fix, and hotfix branches" → "Milestone, fix, and hotfix branches"; naming convention `feature/phase<N>-<short-name>` → `milestone/M-NN-<short-name>` (one PR per milestone, tickets = commits); example commands updated to `milestone/M-03-grade-section-subject` + `feat(milestone): M-03 …` title. ALSO fixed a contradiction the rename exposed: the merge-strategy table said `feature/* → staging | Squash merge | one commit per feature`, but WORKFLOW says tickets = commits and the backlog ledger records each ticket's commit SHA — squashing would destroy per-ticket history; changed milestone/* → staging to "Merge commit (preserve ticket commits)", leaving fix/* and hotfix/* as squash. This merge-strategy change is a semantic call (flagged to Abd.), made for consistency with tickets=commits + the per-ticket SHA ledger. PR-template fix (repo) + this BRANCHING fix (bootstrap) together clear the phase-based branch-naming staleness. | @abdurrehman (with Claude) |
| 2026-05-29 | M-12 backlog drafted (Lecture Mode Viewer + Highlight + Q&A + Voice — 15 tickets T-151..T-165, source Flow 6 v1 §3.1-§3.5, first of four Flow-6 milestones). Pre-draft audit run: Flow 6 split confirmed (M-12 #54/#55/#56/#57-text+voice; M-13 #57 image+vision; M-14 #59/#60/#61; M-15 #58/#71/rating); forward-hook grep folded M-09 "student viewer/rendering → M-12" (core) + exam-prep answer-overlay (T-161); M-08 per-session adaptation hook correctly routed to M-14 (NOT pulled into M-12); predecessor T-150 → M-12 starts T-151 (contiguous). Tickets: session model+lifecycle (T-151), text viewer (T-152), TTS gen+cache (T-153), karaoke player (T-154), hybrid widget v1 text+voice (T-155, canonical reusable — M-13 extends not rebuilds), highlight→prompt+content-element tagging (T-156), misconception/knowledge_gap classifier (T-157), AI answer pipeline Pattern S+web fallback+base persona (T-158), answer side panel (T-159), multi-turn+questions tab (T-160), exam framework overlay on answers (T-161), permissions+#72 privacy (T-162), NATS event emission (T-163), notif+audit+i18n (T-164), E2E+PR (T-165). BLOCKED-HOOK added (T-163): Cognitive DNA interaction-signal consumer → Flow 9/M-18. Scope decisions flagged: hybrid-widget text+voice base here / image+vision M-13 (extend same component); highlights transient here / persistence #58 M-15; base persona only / adaptation #61 M-14; emits events but consumers M-14. Open questions: M-12-relevant (Q1/Q2/Q3/Q12/Q13) built to spec launch recommendations; unresolved ones (Q4/5/7/8/9/10/11) belong to M-13-15 → confirm with Awais before those. ROADMAP M-12 → drafted. | @abdurrehman (with Claude) |
| 2026-05-29 | M-13 backlog drafted (Hybrid Widget + Multimodal Image + Vision LLM — 7 tickets T-166..T-172, source Flow 6 v1 §3.5 image+vision portion, second of four Flow-6 milestones). Pre-draft audit run: no BLOCKED-HOOK targets M-13; forward-hook grep confirmed M-00 §8.22 vision-routing STUB (fleshed out in T-168) + M-12 T-155 hybrid widget (EXTENDED, not forked, in T-167); predecessor T-165 → M-13 starts T-166 (contiguous). Tickets: student_question_image upload profile §11.19 with EXIF strip + per-tenant MinIO + 1yr retention + format/size/count enforcement (T-166), hybrid widget image attachment extending T-155 (T-167), vision-LLM routing fleshing out M-00 §8.22 stub → Groq Llama-3.2-Vision when images present (T-168), vision Q&A integration through the M-12 #56 answer panel + multi-turn (T-169), image-question privacy #72 + retention + vision cost guard (T-170), audit+i18n+notifications+reuse-contract (T-171), E2E+PR (T-172). No new BLOCKED-HOOKs (vision self-contained). Hard constraint enforced in T-167: ONE hybrid widget — M-13 extends the M-12 component, no duplicate input. Flow 5 creation-chat migration onto the shared widget logged as a tracked follow-up (TODO.md), deliberately not forced to avoid disturbing M-09. §3.5 fully specified — no governing open question. ROADMAP M-13 → drafted. | @abdurrehman (with Claude) |
| 2026-05-29 | Reconciled this file per "option b": renamed `_CHANGE_LEDGER.md` → `_CHANGE_LOG.md` and rewrote it to match actual use. VERIFIED FIRST (no blind clearing): every "pending" non-spec edit from the Flow 1-4 review actually landed (content-grep, renumber-agnostic — §6.16→6.19, §8.16→8.20, §8.18→8.22, §9.14→9.21, §11.17/18→11.19; §3.16-3.19/§4.21/cross-grade/independent-signup/promotion/exam-refresh all present); AMENDMENTS Entry 1, TODO deferrals, PERSONA_LEARNING_BATCH_DAYS, STACK_LOCK confirmations present. Stale "pending" body (lines 16-389, listing applied edits as pending using pre-renumber numbers) removed; replaced with a header that states the real purpose + a historical note + renumber map + open-items. AUDIT — post-logging gap: NONE; all recent non-spec/bootstrap edits (Opus 4.8, ticket-status, BRANCHING, resume/close-out, context-bloat, BLOCKED-HOOK, pre-draft audit, M-10..M-13) were already logged in the maintenance-log table (I never actually stopped logging). AUDIT — flow↔non-spec consistency: in line EXCEPT (a) TipTap absent from STACK_LOCK despite finalized Flow 5 asserting it locked [OPEN, flagged], (b) duplicate §3.16 header in ARCHITECTURE [doc bug, flagged]; Yjs/Flow-11 + Google-Calendar/Flow-8 are future-flow items already tracked; SCORING_MODEL / vision-cost-guard env vars are implementation-time (ticket adds ENV_VARS entry per M-00 rule), not drafting misses. PROCESS SAFETY: updated all 4 cross-refs so nothing breaks — backlog/README.md pre-draft-audit grep + append-entry line → `_CHANGE_LOG.md`; AMENDMENTS.md §"What we considered" (removed "deleted after T0 batch", now "retained as _CHANGE_LOG.md"); ARCHITECTURE.md §4 locked-rules cross-ref redirected from the ledger's "New DB tables" section to §3.16 (dual-schema, the authoritative schema-ownership source). BLOCKED-HOOK grep stays satisfied (entries still mention the token + grep path updated). | @abdurrehman (with Claude) |
| 2026-05-29 | Closed the two consistency items the ledger reconcile surfaced. (1) TipTap added to STACK_LOCK §2 Frontend (Concern "Rich-text editor", MIT, core+free extensions only, no Pro/CRDT) with rationale: headless ProseMirror editor that emits JSON stored as lecture_versions.content_jsonb (immutable version-on-save, Flow 5); resolves the finalized-Flow-5-asserts-locked-but-absent inconsistency. Correct home is §2 Frontend — earlier notes mis-said §12 (Change log) and flow-5 §10 lumps it under §4; cross-ref drift, harmless. (2) Duplicate ### 3.16 in ARCHITECTURE.md fixed: the orphan "Deferred to TODO.md" block (nothing referenced it by number) demoted from a numbered header to a bold note under §3.15; §3.16 now uniquely = "Independent users — separate-schema tenant model" (verified single header; all Flow 4/5/TEMPLATE citations to §3.16 resolve correctly). | @abdurrehman (with Claude) |
| 2026-05-29 | M-14 backlog drafted (Live Feedback Panel + NATS Event Pipeline + Per-Session Adaptation — 12 tickets T-173..T-184, source Flow 6 v1 §3.7/§3.8/§3.9, third of four Flow-6 milestones). Pre-draft audit run: no BLOCKED-HOOK targets M-14; forward-hook grep folded the M-12 T-163 event-emission (M-14 builds the consumer pipeline #60), M-12 base-persona (M-14 adds adaptive difficulty #61), and M-08 T-108 per-session-adaptation read-hook (M-14 builds full #61); predecessor T-172 → M-14 starts T-173 (contiguous); verified student_events created in M-00 (M-14 writes to it, no dup) + session_difficulty_log is new (T-175) + AI_ADAPT_QUESTION_THRESHOLD already in ENV_VARS. Tickets: NATS JetStream stream+envelope+subjects (T-173), Analytics Consumer→student_events (T-174), session_difficulty_log+sub-topic tagging (T-175), AI Session Context Consumer (T-176), Live Feedback Consumer→WebSocket (T-177), pipeline backpressure+observability+independent routing (T-178), live feedback metrics computation (T-179), panel UI (T-180), stuck-nudge (T-181), per-session angle-switch adaptation (T-182), permissions+privacy+i18n+notif (T-183), E2E+PR (T-184). Two BLOCKED-HOOKs: (T-179) live-feedback mastery-estimate metric → Flow 9/M-18; (T-174) Cognitive DNA analytics consumer reading student_events → Flow 9/M-18 (M-14 builds the writer/store; DNA reader attaches in Flow 9 — sources the hook M-12 T-163 registered). Forward non-blocked dep: daily-goal metric → Flow 8/M-17 (#69). Key scoping: #60 pipeline = cross-cutting infra (3 independent durable consumers, also serves Flow 8 independent students tagged/routed); #61 angle-switch is session-local (no DNA needed); only long-term difficulty export depends on Flow 9. Open Qs Q5/Q7 built to launch recommendations (10-min stuck, threshold default 2). ROADMAP M-14 → drafted. | @abdurrehman (with Claude) |
| 2026-05-29 | M-15 backlog drafted (Flashcards + Concept Enrichment + Lecture Rating — 10 tickets T-185..T-194, source Flow 6 v1 §3.6/§3.10/§3.11, FOURTH AND FINAL Flow-6 milestone — COMPLETES Flow 6). Pre-draft audit run: no BLOCKED-HOOK targets M-15; forward-hook grep folded M-12 deferred highlight-persistence+auto-flashcards (#58) + rating; predecessor T-184 → M-15 starts T-185 (contiguous). Tickets: highlight persistence model + yellow-mark restore + §5.5 offset-gone edge (T-185), auto-flashcard generation with hash-dedupe (T-186), flashcard.created publish event → Flow 8 SRS (T-187, publish only — py-fsrs deck is M-17), My Highlights/flashcards view (T-188), concept_applications + careers Pakistani vocab + student_simulation_progress + 90-day cache (T-189), concept enrichment generation via Celery concept.enrich_applications cached per-concept (T-190), mini-simulation render + save state (T-191), lecture rating 1-5 optional → 5% into M-10 quality score, anonymous, no student ranking (T-192), permissions+privacy+i18n (T-193), E2E+PR completing Flow 6 (T-194). NO new BLOCKED-HOOKs — all deps on drafted/done milestones (M-09/M-10/M-12) or drafted forward flow (Flow 8/M-17 SRS deck, normal forward dep); Cognitive-DNA path stays the already-registered M-14 analytics → Flow 9 hook. Key boundaries: spaced repetition is Flow 8's (M-15 publishes flashcards only); enrichment cached per-concept not per-student; rating is 5% supplementary to M-10 #32 (95% AI primary); Flow 6 surfaces school-tenant only. Open Qs Q4/Q9/Q10/Q11 built to launch recommendations. ROADMAP M-15 → drafted; FLOW 6 COMPLETE (M-12..M-15 all drafted). | @abdurrehman (with Claude) |
| 2026-05-29 | M-16 backlog drafted (Next-Day Review Loop — 14 tickets T-195..T-208, source Flow 7 v1 §3.1-§3.8, features #39/#40/#44/#45/#46/#47/#48/#49). Pre-draft audit run: Flow 7 spec present (reconstructed earlier); no BLOCKED-HOOK targets M-16; forward-hook grep folded M-09 lecture_type=mini/parent_lecture_id (data ready), M-10 teacher_ai_memory (+reflective_response_pattern category), M-12 content-element-tagged+classified questions, M-14 student_events store (aggregator reads it); predecessor T-194 → M-16 starts T-195 (contiguous). Tickets: review data model + teacher_ai_memory reflective extension (T-195), overnight aggregation beat 23:00 Karachi per-school isolated + #72 filter (T-196), cluster cosine≥0.80 + classify freq≥3/25%+criticality LLM-0-10-threshold-7 + DLQ (T-197), mini-lecture gen reusing Flow 5 pipeline lecture_type=mini web-fallback-off 800-1200w $0.50 (T-198), mini-lecture scoring 7-dim Depth+Alignment×1.5 max-60 (T-199), review card assembly+05:00-SLA+06:00-notify (T-200), review card UI locked layout (T-201), reflective prompt #49 coaching-never-grading→teacher_ai_memory (T-202), drill-down+reply+skip teacher-only-names (T-203), targeted distribution union-of-triggering-students no-auto-quiz cross-grade-blocked (T-204), action buttons (T-205, ordered AFTER distribution to avoid forward dep), review-as-child-record (T-206), permissions+privacy+notif+i18n (T-207), E2E+PR (T-208). THREE BLOCKED-HOOKs: (T-205) Schedule Group Study → Flow 11/M-20 (button disabled until then); (T-204) parent notification on mini-lecture assignment → Flow 10/M-19; (T-195/T-206) review clusters feed Cognitive DNA → Flow 9/M-18 (clusters persisted; DNA reads later). Reuses Flow 5 generation (T-116) + scoring (T-134) as mini variant, no rebuild. Open Qs built to launch recommendations. ROADMAP M-16 → drafted (range T-195 to T-208). | @abdurrehman (with Claude) |
| 2026-05-29 | M-17 backlog drafted (Student Self-Study Mode — 14 tickets T-209..T-222, source Flow 8 v1 §3.1-§3.13, features #62-#70+#72). Pre-draft audit run: Flow 8 spec present; no BLOCKED-HOOK targets M-17; forward-hook grep folded M-14 daily-goal metric (#69, T-211), M-08 adaptation hook (Flow 8 session side, T-210), M-15 flashcard.created (Flow 8 surfaces due cards, T-210); independent students locked here per Flow 4 v3 §3.2. Predecessor T-208 → M-17 starts T-209 (contiguous). RESOLVED the flagged Google-Calendar/GCP item: primary is built-in FullCalendar (STACK_LOCK §2), .ics always available, "Add to Google Calendar" is env-gated user-OAuth (GOOGLE_CALENDAR_SYNC_ENABLED, degrades to .ics) = integration not GCP managed-service; FCM/Brevo/Jazz+Telenor confirmed in STACK_LOCK §notifications (earlier 0-grep was escaping artifact). Tickets: data model+entering-mode+state-persistence+subscription-noop (T-209), session interaction reusing Flow 6 + due-flashcards surface (T-210), daily goal #69 (T-211), material upload 4-types PaddleOCR #62 (T-212), book categorisation+prep-gate #63 (T-213), topic extraction Celery #64 (T-214), conversational study plan #65 (T-215), granularity #66 (T-216), built-in calendar+optional sync #67 (T-217), reminders push/email/SMS #68 (T-218), adherence tracking+alerts #70 (T-219), privacy #72 canonical owner (T-220), permissions+i18n+notif+tenant (T-221), E2E+PR (T-222). FOUR BLOCKED-HOOKs: (T-210) py-fsrs flashcard scheduling+queue #76 → Flow 9/M-18; (T-211) daily-goal signal → Cognitive DNA Flow 9/M-18; (T-219) adherence%→pass-probability #85 Flow 9/M-18; (T-219) parent adherence alert → Flow 10/M-19. Forward non-blocked: subscription tier caps → Flow 13/M-23 (no-op placeholder). CORRECTION TO M-15: M-15 had mislabeled the flashcard boundary ("spaced repetition is Flow 8's domain / py-fsrs deck is M-17") — the spec (Flow 8 Q10 + out-of-scope) is explicit that py-fsrs SCHEDULING is Flow 9/M-18, not Flow 8. Fixed all 5 M-15 references + the 2 "No new BLOCKED-HOOKs" claims → M-15 now carries a proper BLOCKED-HOOK (T-187) for py-fsrs scheduling → Flow 9/M-18 (was an unmarked hook to a blocked flow). ROADMAP M-17 → drafted (range T-209 to T-222). | @abdurrehman (with Claude) |
| 2026-05-29 | Formalized the "Mandatory flow-spec audit" in docs/backlog/README.md (mirrors the milestone pre-draft audit, adapted for specs as the source artifact). 7 steps: (1) verify sources on disk (v2 doc 108 features, #19/20/22 retired; target not already drafted); (2) feature-coverage reconciliation — diff this flow's v2 feature set against all other flow headers so the union = full 1-108 minus retired, catching gaps AND overlaps; (3) BLOCKED-HOOK harvest `grep "BLOCKED-HOOK:" | grep "→ Flow N"` — every hook becomes a real spec section/contract or is re-deferred, never dropped (inverse of milestone "pull inherited hooks"); (4) cross-reference consistency — grep "Flow N" across finalized specs, honor every assertion, resolve ambiguous placements (e.g. #73 Flow 9/11) explicitly; (5) dependency + stack sanity — enforce draft order 9→10→11→12, flag needed STACK_LOCK/DEVIATIONS entries (e.g. Yjs #103); (6) milestone-split + ROADMAP plan (Flow 9 ≈29 features won't fit one milestone); (7) draft to house style + register (ROADMAP blocked→drafted, _CHANGE_LOG, new BLOCKED-HOOKs). Added a per-flow "## Drafting completeness ledger" convention (records features covered, hooks consumed w/ source ticket, cross-refs honored, items re-deferred) so 1-108 coverage + hook registry stay auditable. Also extended the deferral-marker placement note to cover flow specs (which have no tickets): marker goes in the lifecycle section + completeness ledger. No spec drafted yet — this is the gate for Flows 9-12. | @abdurrehman (with Claude) |
| 2026-05-29 | Flow 9 (AI Intelligence Layer / Cognitive DNA) DRAFTED — docs/feature-specs/flow-9-ai-intelligence.md. Ran the new mandatory flow-spec audit: (1) sources on disk OK (v2 doc 108 features, ARCH, STACK_LOCK); (2) feature-coverage reconciliation — Flow 9 owns #42,#43,#74-#99 (28 features); #73 → Flow 11 (resolved); #50 → Flow 6 label-fix (added #50 to flow-6 header); (3) BLOCKED-HOOK harvest — all 12 hooks targeting Flow 9 folded into spec sections + completeness ledger (M-08 schema/bank/diagnostic, M-11 calibration, M-12 interaction consumer, M-14 analytics+mastery, M-15 py-fsrs+queue, M-16 clusters, M-17 py-fsrs+goal+adherence); (4) cross-refs honored (DNA/mistakes/predictions/pass-prob/mastery); (5) deps OK (no upstream flow dep; STACK_LOCK §ML already locks scikit-learn/XGBoost/SHAP/py-fsrs); (6) milestone split recorded; (7) registered. KEY SCOPE: heuristic-first per ARCH §C + STACK_LOCK §ML — all predictive features (#79/#83/#85/#89/#90) built as heuristics in app/services/ml/heuristic/ behind the same API as future models; trained ML = Phase-2 swap (TODO.md, not a flow hook), weekly retrain stubbed. Stack corrections: #94 SM-2 → py-fsrs (FSRS, replaces SM-2); #95 Redis/Bull → Celery ETA + Redis (Bull forbidden). M-18 ROADMAP row converted to family M-18a (bank+collection #42/43/74-76, build-first), M-18b (DNA engine #77-84), M-18c (prediction+recommendations #85-93), M-18d (spaced rep+pedagogy #94-99), order a→b→c→d, status drafted-spec, ticket ranges TBD off T-222. THREE new BLOCKED-HOOKs Flow 9 defers: proactive parent alert #100 → Flow 10/M-19 (Flow 9 emits low-prob signal); DNA aggregate dashboards + class comparison #73 + learning record #105 → Flow 11/M-20; AI-suggested groups #101 consume DNA → Flow 11/M-20. Open Qs adopted launch recs (min-signal threshold, heuristic weights, #73→Flow 11, pedagogy always admin-approved). | @abdurrehman (with Claude) |
| 2026-05-29 | Flow 10 (Parent Monitoring) DRAFTED — docs/feature-specs/flow-10-parent-monitoring.md. Flow-spec audit: (1) not already drafted; (2) coverage = #100 (proactive parent alert) + #105 parent-view portion (student-record core stays Flow 11; #10 parent account/linking stays Flow 4/M-06); (3) BLOCKED-HOOK harvest — all 4 hooks folded (M-06 T-082 read-only monitoring surface §3.1/3.4, M-16 T-204 mini-lecture parent notification §3.3, M-17 T-219 adherence parent alert §3.3, Flow 9 #100 proactive low-prob alert §3.2); (4) cross-refs honored (read-only own-child, aggregate-only self-study per #72, configurable default-ON, parent dashboard lives in Flow 10); (5) deps OK — reads Flow 9 outputs directly (drafted), parent-link gate from M-06; NO forward dep on Flow 11 (parent view + student record are sibling surfaces over the same Flow 9 data); (6) single milestone M-19 (no family — 2 features, rendering+routing); (7) registered. RESOLVED consistency item: M-06 'no independent parent links at launch' vs Flow 8 'independent guardian' → Flow 10 locked to SCHOOL-TENANT at launch; independent-guardian linking documented as future. NO new BLOCKED-HOOKs deferred. ROADMAP M-19 → drafted-spec (deps M-18b/c + M-06 T-082, ticket range TBD off M-18d). Open Qs adopted launch recs (daily escalation; independent-guardian deferred; positive DNA card only). | @abdurrehman (with Claude) |
| 2026-05-29 | Flow 11 (Group Study + Dashboards) DRAFTED — docs/feature-specs/flow-11-group-study-dashboards.md. Flow-spec audit: (1) not drafted, DEVIATIONS.md present; (2) coverage = #73,#101,#102,#103,#104,#105(student core),#106 (7 features; #105 parent-view stays Flow 10); (3) BLOCKED-HOOK harvest — all folded (M-16 T-205 Schedule Group Study → §3.3/M-20a; Flow 9 DNA dashboards+#73+#105 → §3.4-3.7/M-20b; Flow 9 #101 AI groups consume DNA → §3.1/M-20a); (4) cross-refs honored (analytics store read by Flow 11, admin DNA aggregates, migrated-alumni anonymized status, coordinator aggregates); (5) deps OK (Flow 9 drafted; reuses Flow 6/8 sessions + M-10 #38/#35/#36 — composes not rebuilds); (6) split into M-20a Group Study + M-20b Dashboards; (7) registered. RESOLVED: #73 placement → Flow 11 (fixed flow-8 TODO line); #104/#106 vs M-10 #38 overlap → compose/link not rebuild; collaborative-notes mechanism → Yjs CRDT. DEVIATION added: yjs (CRDT) for group_notes only (DEVIATIONS.md entry + change-log row + STACK_LOCK §2 row; TipTap note refined to cross-ref; lecture editing stays non-CRDT). Threads/presence ride existing FastAPI WS + Redis (no deviation); geographic heatmap uses ECharts (already locked). ROADMAP M-20 → family M-20a (group study #101/102/103 + scheduler, incl Yjs) + M-20b (dashboards #73/104/105-student/106), order a→b, drafted-spec, deps M-18b/c (+ M-10 T-139 for b). NO new BLOCKED-HOOKs deferred (Flow 12 reads Flow 11 but isn't a dependency). Open Qs adopted launch recs (group cap 8; Yjs notes; #73 Flow 11; #38 compose-not-rebuild). | @abdurrehman (with Claude) |
| 2026-05-29 | Flow 12 (AI Chatbot + Virtual Assistant) DRAFTED — docs/feature-specs/flow-12-chatbot-va.md. THE FINAL FLOW — all 12 flows now drafted. Flow-spec audit: (1) not drafted; (2) coverage = #107 (always-available chatbot) + #108 (proactive VA) → completes full v2 set; (3) BLOCKED-HOOK harvest — ZERO inbound (nothing ever deferred to Flow 12; pure downstream surface); (4) cross-refs — only Flow 11's downstream note (VA reads Flow 11 output, not a dependency); (5) deps OK — chatbot reuses RAG stack (Flow 5/6), VA composes Flow 9/7/8/11 signals (all drafted), terminal flow so no forward deps; (6) split M-21a Chatbot (#107, self-contained RAG) + M-21b VA (#108, proactive aggregator + va_memory + action-drafting); (7) registered. KEY: Flow 12 builds NO new intelligence/retrieval — chatbot reuses RAG (role+tenant scoped, source badges, never out-of-scope), VA composes existing signals only. CXO rule anchored: VA actions are suggest-and-confirm ONLY, never autonomous (routed through owning flow). NEW va_memory + chatbot_conversations tables; mode=chatbot events feed Flow 9 respecting #72. ROADMAP M-21 → family M-21a (chatbot, deps RAG M-09/M-12) + M-21b (VA, deps M-18 + Flow 7/8/11 + M-21a), order a→b, drafted-spec. NO new BLOCKED-HOOKs deferred (terminal). CAPSTONE COVERAGE PROOF: union of all 12 flow headers = full 1-108 minus retired {19,20,22} = 108 features covered, ZERO missing, ZERO overlaps (#50 label-fixed onto Flow 6, #73→Flow 11, #105 split Flow10-parent/Flow11-student). ALL FLOWS 1-13 DRAFTED; remaining roadmap work is the blocked milestone families M-18a-d / M-19 / M-20a-b / M-21a-b (specs ready) + M-22 (Flow 2 promotion) + M-23 (Flow 13 subs). | @abdurrehman (with Claude) |
| 2026-05-29 | ROADMAP timeline re-baselined to Claude-Code velocity (tiered). Hamza implements via Claude Code (time = review + targeted debug/reimplement, not authoring). Heavy RAG/voice/ML milestones (M-00, M-09, M-12, M-14, M-18a-d) compressed ~2-3×; all other CRUD/UI milestones ~4×. Updated every Est.duration cell (29 milestones), the timeline ASCII annotations, both summary lines, and added a stated basis line atop the Timeline view (re-tunable). Net: launch-ready core 7-9 months → ~2-3 months; M-16-M-21 +~1-1.5 months; FULL V2 LAUNCH ~3-4.5 months total (was ~10-14 months hand-coding). Relative sizing + dependency order preserved. | @abdurrehman (with Claude) |
| 2026-05-29 | Added milestone-status roll-up to scripts/check_ticket_status.py (the CI verifier). It now derives each milestone's status from its ticket Status fields (all done->done; any done->in-progress; else pre-implementation, no assertion) and fails the PR if the ROADMAP Status column disagrees at the done/in-progress end — never fighting the pre-impl drafting labels (blocked/drafted-spec/drafted). Runs on EVERY PR incl. docs-only (a ROADMAP/ticket edit is docs-only); the referenced-tickets-done gate still skips docs-only. Tested (detects all-done + partial mismatch; skips not-started). Makes the ROADMAP milestone Status an auto-verified roll-up of ticket truth instead of a hand-maintained field (answers "does CC update this" — no, the CI now does). Normalized ROADMAP Status to a stated vocabulary (blocked->drafted-spec->drafted->in-progress->done) + added the vocabulary note + auto-verified note under the summary table; fixed stale state: M-00 -> done, M-01 -> in-progress, M-02/M-22/M-23 'todo' -> 'drafted'. Updated CI snippet comment + TICKET_STATUS_AUTOMATION.md verifier row. | @abdurrehman (with Claude) |
| 2026-05-29 | FOUNDATION HARDENING (A–F) — closes the FE-quality / FE↔BE-integration / migration / UX enforcement gap that let M-00/M-01 ship broken nav + ToS despite correct tickets (root cause = enforcement gap, not content gap). (A) STACK_LOCK §2: locked Vitest+RTL + Playwright (FE testing), openapi-typescript (generated FE types), `response_model=` mandatory on every endpoint; reconciled the entire open-items list (8 RESOLVED incl. design-tokens→tailwind.config.ts, i18n key structure, file-upload=presigned-direct-to-MinIO, /api/v1 versioning, FE-folder=feature-based, audit-log, multi-tenancy, chunking; 8 DEFER with owner Abd. + resolve-before tag: NATS/WS→M-12, Celery→M-14, pooling/embedding/PII/rate-limit/backup→pilot; 1 NEEDS-TRIAGE: Qdrant→M-09). (B) Migration alignment to the already-locked ARCH §4.12 (model-first autogenerate, one-concern) — no new policy; tickets' DDL reframed as intent. (C) README ticket template hardened: added API-contract / Tests / UX-acceptance required fields + migration-intent rule + a pre-draft-audit "open-item + ticket-modernization gate". (D) New CI snippet ci.frontend-checks.snippet.yml: frontend-unit (Vitest), typed-client-drift, response-model-gate, e2e-smoke (Playwright smoke-on-PR, full nightly). (E) seed_dev.py specified (M-01a). (F) .claude/CLAUDE.md (primary CC carrier): tightened endpoint/UI/migration decision-rules, added FE-tests-mandatory + generated-typed-client + model-first + ticket-modernization-check + CI invariant 7; WORKFLOW §2.1 + implement step updated as the fuller reference. ARCHITECTURE §12.4/§2.13 changed (types generated, not hand-mirrored) under AMENDMENTS A-002 (process-correct; pulls TS-type-gen forward from Phase 2). New milestone M-01a (T-223–T-235) inserted after M-01 (M-02 deps→M-01a): 7 foundation tickets + 6 audit-then-fix tickets remediating M-00/M-01 (nav reachability, ToS render/scroll/accept, typed-client regen, migration reconcile, FE-test backfill). M-03→M-17 NOT rewritten — they inherit the gates via CLAUDE.md + CI, each modernized just-in-time at its turn. All additions lightweight, pnpm-native, no forbidden libs. Base rebuilt from user's uploaded latest (7 files) + WORKFLOW/M-02/CLAUDE from staging. | @abdurrehman (with Claude) |
| 2026-05-29 | Added scripts/modernize_milestones.py — idempotent tool that prepends a "hardened-template gates apply (post-M-01a)" banner after the H1 of each pre-hardened milestone backlog (M-03..M-17). The banner makes explicit, per-file, that the CLAUDE.md + CI gates bind every ticket regardless of wording (data-model = intent/model-first per ARCH §4.12; response_model + generated typed client per A-002; FE tickets need Vitest+RTL + Playwright E2E + UX-acceptance). Does NOT rewrite ticket bodies or add per-ticket fields (those stay just-in-time at implementation). `--check` mode for CI/verification. Token-efficient + safe: run on the current local files (no stale-file clobber); re-runnable. M-00/M-01 remediated by M-01a; M-01a/M-02 already on the new template; M-18+ are drafted-spec; M-22/M-23 handled at ID reassignment. | @abdurrehman (with Claude) |
| 2026-05-29 | Added M-01a T-235 (Container profiles + dev ergonomics) — local stack was running all 16 containers from M-00, slowing Hamza's machine. Solution: Compose profiles (core ~7 always-on: postgres/redis/authentik×3/api/frontend; opt-in groups rag=qdrant/infinity/minio, workers=celery×2, events=nats, observability=prometheus/grafana/loki off by default) + `mem_limit`s on heavy services + `docs/DEV_CONTAINERS.md` milestone→profile map. KEY grounded constraint: §16.1 lifespan connects postgres/redis/NATS EAGERLY at startup (qdrant/minio are lazy), so NATS can't be cleanly profiled off — added an `EVENTS_ENABLED` flag (default false) that no-ops `init_nats()`, paired with the `events` profile (on from M-12). PR+demo ticket renumbered T-235→T-236; M-01a range now T-223–T-236; ROADMAP updated. No topology change (still single root docker-compose.yml, no Makefile per T-002). | @abdurrehman (with Claude) |
| 2026-05-29 | Folded 3 of the 5 M-01 "known violations (Abd review)" into existing M-01a tickets instead of follow-up tickets: (T-225, renamed + estimate 0.5→1d) add generic `SuccessEnvelope[T]` in core/responses.py + rewire all ~25 routes off `response_model=dict` to typed `SuccessEnvelope[XxxRead]` + add `operation_id` to all ~30 routes/9 routers + extend check_response_model.py to gate both (closes the response_model=dict + missing operation_id violations — required so the A-002 typed client generates real types, not dict); (T-233) backfill §4.12 Purpose/Risk/Reversible headers on all 11 existing migrations during the model-first reconciliation. Remaining two violations unchanged: §3.13 cross-tenant denial test correctly deferred to M-02 (first per-school data); T-026 Playwright "deviation" auto-resolved (now locked in STACK_LOCK §2). | @abdurrehman (with Claude) |
| 2026-05-29 | Rewrote scripts/TICKET_STATUS_AUTOMATION.md to verifier-only — the prior version over-promised a per-commit writer hook (update_ticket_status.py + .githooks/post-commit + core.hooksPath) that was evaluated and SHELVED; only the read-only CI verifier + milestone roll-up was adopted (confirmed against repo: no core.hooksPath, no writer files, verifier job present + required). Dropped the writer "Files" rows, the Install/core.hooksPath section, the Closes-Ticket trailer convention, and the division-of-labour section; kept check_ticket_status.py (verifier + roll-up), the CI-job, the WORKFLOW/CLAUDE close-out as how status is written, the status vocabulary, and honest limitations. Closes the session-7 "revert shelved writer-hook references" debt. | @abdurrehman (with Claude) |
| 2026-05-29 | Item 1 (skills as the "how-to" carrier): updated frontend-master SKILL.md — Rule 9 now mandates generated API types from schema.d.ts/openapi-typescript (A-002, never hand-mirror); NEW Rule 12 (tests ship with the component: Vitest+RTL four-states + Playwright @smoke acceptance, examples, 60% coverage) and NEW Rule 13 (every page reachable from app-shell nav + renders real content + scrolls — the M-01 orphan/blank-page failure class); rule-count + description updated (now Rules 1–13). Then trimmed the overlapping body rules in .claude/CLAUDE.md (#6 FE-type clause, #7 nav/scroll/tests, step 5) and docs/WORKFLOW.md (implement step + §2.1 page-reachable checkbox) to a one-line gate + a pointer to the frontend-master rule — keeping the binary gates in CLAUDE/CI and the worked patterns in the skill (no duplication, leaner carriers). Skill-routing table in CLAUDE.md already routes frontend-master + "skills win on shared concerns", so CC loads the patterns at authoring time. | @abdurrehman (with Claude) |
| 2026-05-29 | Item 2 (close the model-authoring skill gap): added NEW skill .claude/skills/data-modeling/SKILL.md — the constructive "how to shape a model right" patterns from ARCHITECTURE.md §4, with correct/WRONG examples: UUIDv7 PKs + the three composed mixins (Audit/SoftDelete/Tenant), plural-table/singular-FK naming, NOT NULL default, explicit ON DELETE + index on every FK, native-enum-vs-reference-table, timestamptz-UTC tz-aware timestamps, JSONB-only + Pydantic shape (child table for relational), DB-level CHECK/UNIQUE + explicit string lengths, justified+named indexes, soft-delete≠cascade + the immutable versioning pattern, and the …Read-schema-subset-of-model alignment note. Rationale: routine features/<x>/models.py work does NOT trigger stack-enforcer (stdlib + approved deps only), so there was no skill loading when CC authors a model — exactly the T-230-class gap. This is the AUTHORING layer; phase-complete-review §4.20 remains the PR-time AUDIT layer (§4.22 #14), stack-enforcer keeps layer-purity/imports — no overlap. Registered in CLAUDE.md skill table (3→4 skills) with a narrow models.py trigger. | @abdurrehman (with Claude) |
| 2026-05-29 | Item 3 (audit→improvement feedback loop): created docs/AUDIT_LOG.md — an APPEND-BY-CLASS root-cause log (one entry per generalizable mistake-class, occurrences accumulate under it) with a strict inclusion bar (log only systemic classes a carrier could prevent + would recur; explicitly NOT per-PR pass/fail, CI status, or one-off fixes — those stay in phase-complete-review's transient checklist), an entry schema (class · occurrences · existing-rule-when-seen? · target-carrier · status · promoted-rule), and a dedupe rule. Seeded with the 7 classes already diagnosed this session (api-contract×2, fe-types, fe-nav, fe-render, test-gap, migration), all marked promoted to the carriers built this session — so the log records that the loop already ran once. Wired the loop: (a) phase-complete-review skill gained a "capture systemic findings to AUDIT_LOG" output step = the always-on per-PR capture engine (the only durable capture for milestones after M-01a, which lack audit-fix tickets); (b) backlog/README gained a "Milestone-boundary audit-log distillation" section (scan recurring classes → promote to carrier with the right bindingness → verify via recurrence-after-rule, owner Abd.); (c) M-01a's audit-fix tickets (T-230–T-235) seed the log at close-out (also fixed a stale "Two ticket groups" range left by the T-230 renumber: T-231–T-236 → correct T-230–T-235 + T-236 containers + T-237 PR). | @abdurrehman (with Claude) |
| 2026-06-09 | Distillation pass triggered by the M-01a create-flow bug report (every admin create 422'd live while all tests/CI stayed green). Confirmed on branch milestone_1a: generated types were correct (ExamSyllabusCreate has exam_board, no description) but api-client *request* types were hand-written (syllabiApi.create typed `{name,description}`; tiers create sent applies_to_role + omitted slug) — a CONSUMPTION bug, not generation; reads used generated types, creates didn't. Root cause it slipped: `typed-client-drift` checks schema FRESHNESS not CONSUMPTION, Rule 9 was prose-only, and every test mocked the contract (mock-api.ts old shape) so green ≠ working. Carrier hardening: (1) AUDIT_LOG — fe-types marked `recurred-after-rule` (2026-06-09) + two new classes `error-envelope-parsing` and `mock-contract-drift`; (2) frontend-master Rule 9 now mandates request bodies typed from the generated `*Create`/`*Update` schema (exact WRONG example) + correct error-envelope parsing (`body.error.*` + FastAPI 422 detail[]) + mandatory `onError`; Rule 12 now requires create/update `@smoke` to hit the REAL seeded backend (never mock the contract) + a contract test vs schema.d.ts; (3) CLAUDE.md CI invariant 7 extended; (4) new CI snippet ci.contract-gates.snippet.yml — `client-request-type-lint` + `contract-test` jobs + a note to de-mock the e2e-smoke create paths. The lint script, contract test, and de-mocked create smoke are delivered by Hamza's M-01a fix (red-then-green proof obligation). | @abdurrehman (with Claude) |
| 2026-07-07 | M-07b backlog drafted (Custom Login + Unified Auth Hub — 10 tickets T-238..T-247). UX pivot: replace Authentik UI redirect with BFF custom login (`POST /api/v1/auth/login` email+password → Authentik server-side → HttpOnly cookies), unified `/signup` hub routing invite/independent/parent paths, forgot-password + reset-password custom UI, session migration off sessionStorage, OIDC redirect demoted to `AUTH_LOGIN_MODE=oidc_redirect` fallback. Requires ARCH §6.4 amendment (T-238). Depends on M-00/M-01 auth + M-05/M-06 signup flows for hub routing. ROADMAP row inserted after M-07. | @shaabii (with Claude) |